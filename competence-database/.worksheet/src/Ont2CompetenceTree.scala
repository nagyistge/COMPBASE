import uzuzjmd.competence.service.rest.CompetenceServiceRest
import scala.collection.JavaConverters._
import org.scalaide.worksheet.runtime.library.WorksheetSupport._


object Ont2CompetenceTree {; def main(args: Array[String])=$execute{;$skip(167);
 		val stuff = List.empty[String];System.out.println("""stuff  : List[String] = """ + $show(stuff ));$skip(26); val res$0 = 
 		stuff.forall(x=>false);System.out.println("""res0: Boolean = """ + $show(res$0))}
}
